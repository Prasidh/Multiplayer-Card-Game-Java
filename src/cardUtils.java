package src;
public class cardUtils {

	public int getWinner(Card [] cards) {
		
		int len = cards.length;
		int [] vals = new int[len];
		
		int max;
		//First, we determine which indices contain the max value
		for(int i = 0; i<len; i++) {
			if(i == 0) {
				//max = cards.value
			}
		}
		
		//Next, we compute which of the winning indices contain the best suit
		
	}
}
